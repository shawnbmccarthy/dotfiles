[General]
# SDDM greeter injects these at runtime; list them so qmllint/qmlls do not
# treat every `config.foo` / `sddm.foo` as invalid unqualified access.
disableUnqualifiedAccess = "config,sddm,keyboard"
